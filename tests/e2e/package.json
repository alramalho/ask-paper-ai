{
  "name": "e2e-tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "@types/node": "^20.3.2"
  },
  "scripts": {
    "start": "npx playwright test --headed",
    "start-local": "export APP_URL=http://127.0.0.1:3000 && export ENVIRONMENT='dev' && npx playwright test --headed",
    "start-sandbox": "export APP_URL=https://sandbox--ask-paper.netlify.app/ && export ENVIRONMENT='sandbox' && npx playwright test"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
