{
  "name": "e2e-tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "@types/node": "^20.3.2"
  },
  "scripts": {
    "start": "npx playwright test",
    "start-local": "source ../scripts/load_dotenv.sh .env.local && yarn start --headed",
    "start-sandbox": "source ../scripts/load_dotenv.sh .env.sandbox && yarn start"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "next-auth": "^4.19.1"
  }
}
